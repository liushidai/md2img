## 1. 共享协议与后端存储准备

- [x] 1.1 在 `packages/shared` 定义当前文档读取与保存所需的共享类型与协议
- [x] 1.2 为 `apps/server` 引入 Bun 兼容的 SQLite 依赖，并创建数据库初始化与最小 `documents` 表建表逻辑
- [x] 1.3 约定单文档读取语义，确保数据库为空时也能返回可编辑的空文档结果

## 2. 后端文档模块

- [x] 2.1 在 `apps/server/src/modules/document` 下创建 route、service、schema 结构
- [x] 2.2 实现当前文档读取接口，并返回共享层约定的数据结构
- [x] 2.3 实现当前文档手动保存接口，只持久化 Markdown 原文与时间字段
- [x] 2.4 在服务入口注册 document 模块，并保留现有 health 接口可用

## 3. 前端编辑与预览页面

- [x] 3.1 在 `apps/web/src/features/editor` 下创建 editor API 与页面所需组件结构
- [x] 3.2 实现左侧 Markdown 编辑区、右侧 HTML 预览区与顶部状态栏布局
- [x] 3.3 接入 Markdown 本地渲染与 sanitize 链路，确保危险 HTML 不会在预览区执行
- [x] 3.4 实现标题推导、空预览占位与加载失败/保存失败提示

## 4. 页面状态与保存交互

- [x] 4.1 在前端维护“最近一次成功保存内容”与当前编辑内容，驱动已保存/未保存/保存中状态
- [x] 4.2 实现首次加载回显、手动保存按钮启用规则与保存成功后的状态更新
- [x] 4.3 实现保存失败保留内容并允许重试的交互
- [x] 4.4 实现存在未保存内容时的浏览器原生离开提醒

## 5. 校验与收尾

- [x] 5.1 补充必要的类型整理与代码清理，确保前后端共享协议引用一致
- [x] 5.2 运行 `bun run typecheck:web`、`bun run typecheck:server` 与前端 lint/构建相关校验
- [x] 5.3 手工验证单文档加载、编辑、预览、保存、刷新回显与失败提示主链路

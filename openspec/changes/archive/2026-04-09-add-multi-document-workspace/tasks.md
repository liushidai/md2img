## 1. 共享协议与数据模型升级

- [x] 1.1 将共享层从单文档 `CurrentDocument` 升级为文档列表、文档详情、创建与更新协议
- [x] 1.2 升级后端 SQLite `documents` 表结构，支持 `id`、`title`、`markdown_content`、`created_at`、`updated_at`
- [x] 1.3 为旧的单文档数据制定迁移或兼容策略，确保现有首篇文档不会在升级时丢失

## 2. 后端文档资源接口

- [x] 2.1 重构 document 模块 schema 与 service，使其支持多文档查询与按 ID 读写
- [x] 2.2 新增 `GET /api/documents` 与 `POST /api/documents`，支持文档列表和新建文档
- [x] 2.3 新增 `GET /api/documents/:id` 与 `PUT /api/documents/:id`，支持按文档详情加载与保存
- [x] 2.4 移除或替换旧的 `/api/documents/current` 单文档语义，并保证前后端一起迁移

## 3. 前端工作台壳层与路由

- [x] 3.1 在前端引入 `vue-router`，建立 `/documents/:id` 路由与工作台入口
- [x] 3.2 新增工作台壳层组件，承载左侧文档列表与右侧编辑区
- [x] 3.3 实现文档列表加载、按 `updatedAt` 倒序展示与当前选中态高亮
- [x] 3.4 实现“新建文档”动作，并在创建成功后跳转到新文档 URL

## 4. 编辑器与交互迁移

- [x] 4.1 将编辑器从单文档状态改为按文档详情加载，并支持独立 `title` 输入框
- [x] 4.2 调整保存逻辑，使其同时提交当前文档的 `title` 与 `markdownContent`
- [x] 4.3 保留现有 Markdown 本地预览与 sanitize 链路，确保多文档下预览行为不回退
- [x] 4.4 实现切换文档时的未保存确认，并区分浏览器离开提醒与工作台内部切换确认

## 5. 校验与规格同步

- [x] 5.1 更新前端与后端中围绕 `current` 的命名和类型引用，保证资源型语义一致
- [x] 5.2 运行相关类型检查、前端 lint 与构建校验，确认多文档工作台可以编译通过
- [x] 5.3 手工验证列表、新建、切换、按 URL 进入文档、保存与未保存保护主链路
